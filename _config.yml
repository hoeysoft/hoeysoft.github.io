# This is not complete.
# Keep chaning with understanding 'jekyll'
exclude:
    [Gemfile, Gemfile.lock, Guardfile]

highlighter: pygments
timezone: Asia/Seoul
defaults:
    -
        scope:
            path: ""
            type: "posts"
        values:
            layout: "default"
